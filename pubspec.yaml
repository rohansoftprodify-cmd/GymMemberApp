name: gym_member_app
description: Gym member mobile app linked to a single gym.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^16.2.4
  supabase_flutter: ^2.10.2
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  geolocator: ^13.0.4
  mobile_scanner: ^6.0.11
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
